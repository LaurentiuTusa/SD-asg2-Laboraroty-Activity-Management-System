@model IEnumerable<Asg2.DAL.Models.Attendance>

@{
    ViewData["Title"] = "ShowAttendance";
    Layout = "~/Views/Shared/Teacher.cshtml";
}

<h1>ShowAttendance</h1>

<p>
    <a asp-action="CreateAttendance">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LabId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LabId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentId)
            </td>
            <td>
                @*@Html.ActionLink("NEdit", "Edit", new { /* id=item.PrimaryKey */ }) |*@
                @*@Html.ActionLink("NDetails", "Details", new { /* id=item.PrimaryKey */ }) |*@
                @Html.ActionLink("Delete", "DeleteA", new { id=item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
